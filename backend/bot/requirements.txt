# The bot service (Root Directory = backend/bot) must install the FULL backend
# dependencies (asyncpg, feedparser, imageio-ffmpeg, deep-translator, boto3, ...).
# Previously this was a "lightweight" list which broke /news, live headlines and /mp4c.
-r ../requirements.txt
